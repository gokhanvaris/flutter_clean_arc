import 'package:flutter/material.dart';

class BuildLoginBody extends StatelessWidget {
  const BuildLoginBody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("LoginView"),
    );
  }
}
